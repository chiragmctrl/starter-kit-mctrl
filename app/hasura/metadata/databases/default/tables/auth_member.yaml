table:
  name: member
  schema: auth
object_relationships:
  - name: organization
    using:
      foreign_key_constraint_on: organizationId
  - name: user
    using:
      foreign_key_constraint_on: userId
